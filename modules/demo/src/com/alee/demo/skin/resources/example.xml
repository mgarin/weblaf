<skin>

    <!-- Toolbar text -->
    <style type="toolbar" id="tool" extends="attached" padding="4" />
    <style type="label" id="tool" margin="0,6,0,0" />
    <style type="toolbarseparator" id="tool" margin="0,4,0,4" />

    <!-- Example information bar -->
    <style type="panel" id="info-bar" extends="decorated" padding="6,6,6,6">
        <painter>
            <decorations>
                <decoration>
                    <WebShape round="0" sides="0,0,1,0" />
                    <WebShadow type="outer" width="0" />
                    <LineBorder color="170,170,170" />
                    <ColorBackground color="white" />
                </decoration>
            </decorations>
        </painter>
    </style>

    <!-- Panel with all previews -->
    <style type="panel" id="previews">
        <painter>
            <decorations>
                <decoration>
                    <WebShape round="0" />
                    <WebShadow type="inner" width="15" />
                    <ColorBackground color="white" />
                </decoration>
            </decorations>
        </painter>
    </style>

    <!-- Skin selectors panel -->
    <style type="panel" id="skin-selectors">
        <painter>
            <decorations>
                <decoration>
                    <WebShape round="0" sides="0,0,1,0" />
                    <LineBorder color="170,170,170" />
                    <ColorBackground color="white" />
                </decoration>
            </decorations>
        </painter>

        <!-- Skin selector button -->
        <style type="togglebutton" id="skin-selector">
            <painter>
                <decorations>
                    <decoration>
                        <WebShape round="0" />
                        <WebShadow type="outer" width="0" />
                    </decoration>
                </decorations>
            </painter>
        </style>

    </style>

</skin>